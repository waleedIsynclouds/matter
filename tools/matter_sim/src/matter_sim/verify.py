from __future__ import annotations


def run_verify() -> None:
    raise SystemExit(
        "matter-sim verify is intentionally a documented placeholder. "
        "Build the CHIP Python controller with `./scripts/build_python.sh` "
        "inside connectedhomeip, then use chip-repl/chip-tool to independently "
        "commission and read attributes from the simulator."
    )
